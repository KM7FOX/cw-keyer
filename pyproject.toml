[project]
name = "km7fox-cw"
version = "0.1.0"
description = "CW/Morse sending and decoding tools"
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"